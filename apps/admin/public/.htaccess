Options -MultiViews
RewriteEngine On

# Serve real files and folders normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# React Router routes (for example /products) must load the SPA entry point.
RewriteRule ^ index.html [L]
