{
  "name": "ramanstore-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local --watch src/app.js",
    "start": "node --env-file-if-exists=.env.local src/app.js",
    "bundle:hostinger": "esbuild src/app.js --bundle --minify --platform=node --format=esm --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" --outfile=src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^2.0.2",
    "mysql2": "^3.23.3",
    "nodemailer": "^9.0.5",
    "pg": "^8.23.0"
  }
}
