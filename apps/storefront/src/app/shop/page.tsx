import Link from "next/link";import{ProductGrid}from"../ui/ProductGrid";
const filters=[["All",""] ,["Women","female-wear"],["Kids","kids-wear"],["Jewellery","artificial-jewellery"]];
export default function ShopPage({searchParams}:{searchParams:{category?:string;q?:string}}){const active=searchParams.category||"";return <main className="container shopPage"><div className="sectionHeading"><div><span className="kicker dark">The full collection</span><h1>{searchParams.q?`Search results for “${searchParams.q}”`:"Find your next favourite"}</h1><p className="shopIntro">Explore fashion and finishing touches selected for quality, comfort and effortless style.</p></div></div><div className="filterRow">{filters.map(([label,value])=><Link key={label} className={`filterChip ${active===value?"active":""}`} href={value?`/shop?category=${value}`:"/shop"}>{label}</Link>)}</div><ProductGrid category={active} q={searchParams.q}/></main>}
